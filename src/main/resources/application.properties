# Nombre de la aplicación
spring.application.name=Inventrack

# Importa ficheros separados (opcional si no existen)
spring.config.import=optional:classpath:application-datasource.properties,optional:classpath:application-jpa.properties

# DataSource (usar variables de entorno para valores sensibles)
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres
#$ {DB_URL:jdbc:postgresql://localhost:5432/postgres}
spring.datasource.username=postgres.qwzwfergpjeffdnmjzvp
#$ {DB_USERNAME:postgres}
spring.datasource.password=Inventrack2025+
#$ {DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect